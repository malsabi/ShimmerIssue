<?xml version="1.0" encoding="utf-8" ?>
<Border xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        xmlns:shimmer="clr-namespace:Syncfusion.Maui.Toolkit.Shimmer;assembly=Syncfusion.Maui.Toolkit"
        xmlns:models="clr-namespace:ShimmerIssue.Models"
        xmlns:viewModels="clr-namespace:ShimmerIssue.ViewModels"
        x:Class="ShimmerIssue.StudentTemplate"
        x:DataType="models:StudentModel"
        Background="White"
        Padding="20">

    <shimmer:SfShimmer Background="Transparent"
                       WaveColor="CornflowerBlue"
                       IsActive="{Binding IsBusy, Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, x:DataType=viewModels:MainViewModel}">

        <shimmer:SfShimmer.CustomView>
            <Grid RowDefinitions="Auto, Auto, Auto, Auto" RowSpacing="10">
                <shimmer:ShimmerView Grid.Row="0"
                                     ShapeType="RoundedRectangle"
                                     HeightRequest="20" />
                <shimmer:ShimmerView Grid.Row="1"
                                     ShapeType="RoundedRectangle"
                                     HeightRequest="20" />
                <shimmer:ShimmerView Grid.Row="2"
                                     ShapeType="RoundedRectangle"
                                     HeightRequest="20" />
                <shimmer:ShimmerView Grid.Row="3"
                                     ShapeType="RoundedRectangle"
                                     HeightRequest="20" />
            </Grid>
        </shimmer:SfShimmer.CustomView>

        <shimmer:SfShimmer.Content>
            <Grid RowDefinitions="Auto, Auto, Auto, Auto" RowSpacing="10">
                <Label Grid.Row="0" Text="{Binding Name}" />
                <Label Grid.Row="1" Text="{Binding Email}" />
                <Label Grid.Row="2" Text="{Binding Password}" />
                <Label Grid.Row="3" Text="{Binding Phone}" />
            </Grid>
        </shimmer:SfShimmer.Content>
    </shimmer:SfShimmer>
</Border>